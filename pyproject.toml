[project]
name = "cc-python"
version = "0.1.0"
description = "Cookiecutters for python projects @singularIT"
requires-python = ">=3.12"
dependencies = [
    "cookiecutter>=2.7.1",
    "pre-commit>=4.6.0",
]

[dependency-groups]
dev = [
]
